import { LangTranslatorPipe } from './lang-translator.pipe';

describe('LangTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new LangTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
